#include<stdio.h>
int main(int argc, char const *argv[])
{
    int num = 20;// variable : init
    int age;
    printf("Enter the age :");
    scanf("%d",&age);
    if(20%2 == 0){
         printf("value for num is :%d, age is :%d",num,age);
    }
   
    return 0;
}
